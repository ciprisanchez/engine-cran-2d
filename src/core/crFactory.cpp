#include "core/crFactory.h"
/*
void CranFactory::create_scene(CR_SCENE_MODE p_mode, int p_speed)
{
    //CranScene* cs = new CranScene();
/*    cs->_screenArea = new Rectangle(p_screenArea->_x1, p_screenArea->_y1, p_screenArea->_x2, p_screenArea->_y2);
    cs->_mode = p_mode;
    cs->_status = PLAYING;
    cs->_totalFrames = p_animation->_vFrames.size();
    
    // Animation speed
    cs->_speedCounter = 0;
    cs->_speed = p_speed;

}*/